/**
 * Babelute Fondamental Structure : this is ALL you need to do everything.
 * 
 * There is in fact two solutions to the question of the universe and everything else... : 42 and this. ;)
 *
 * Go learn Babelute and what to do with it, and come back later to fully appreciate why sooo much things, 
 * and even more, is derivated from those so simple and minimalistic lines of code. 
 */

// ES7 Style
class Babelute {
	_lexems = [];
	_append = (lexic, name, args) => {
		this._lexems.push({
			lexic: lexic,
			word: word,
			args: args
		});
		return this;
	}
}